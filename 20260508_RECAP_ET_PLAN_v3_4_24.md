# RÉCAP & PLAN D'ACTION v3.4.24
**Date :** 8 mai 2026
**Production (main) :** v3.4.18 — https://amazon.foliow.app
**Staging :** v3.4.23 — https://d9xny9istvl53.cloudfront.net
**Preprod :** v3.4.23 — https://preprod.amazon.foliow.app

---

# PARTIE A — RÉCAP SESSION DU 8 MAI 2026

## A.1 Chantier principal — Découverte technique Vendor Central

Session entièrement dédiée au diagnostic et à la résolution du remplissage des champs VC via Claude in Chrome.

### Chronologie des découvertes

| Étape | Découverte | Impact |
|---|---|---|
| 1 | `fillField()` sur `textarea` natif écrit dans le DOM mais React ignore au submit | Méthode invalide |
| 2 | Les champs sont des web components `kat-textarea`/`kat-input` Katal | Sélecteurs à changer |
| 3 | `kat-textarea.value = val` propage au textarea interne mais React ignore au submit | Méthode invalide |
| 4 | `execCommand('insertText')` + `blur` sur le textarea interne (shadow DOM) → React capture la valeur | ✅ **MÉTHODE VALIDE** |
| 5 | Les bullets 1-4 n'existent pas dans le DOM au chargement — clic x4 "Ajouter plus" requis | Étape obligatoire |
| 6 | Sauvegarde via `kat-button[label="Enregistrer et terminer"].shadowRoot.querySelector("button").click()` | ✅ validé |
| 7 | Intercepteur XHR pour confirmer `{"status":"SUCCESS"}` | ✅ pattern identifié |

## A.2 Méthode finale validée — `fillAndBlur()`

```javascript
function fillAndBlur(name, val) {
  var el = document.querySelector("kat-textarea[name='" + name + "'], kat-input[name='" + name + "']");
  if (!el) { console.warn("NON TROUVÉ:", name); return; }
  var inner = el.shadowRoot
    ? (el.shadowRoot.querySelector('textarea') || el.shadowRoot.querySelector('input'))
    : null;
  if (!inner) { console.warn("INNER NON TROUVÉ:", name); return; }
  inner.focus();
  inner.click();
  document.execCommand('selectAll');
  document.execCommand('delete');
  document.execCommand('insertText', false, val);
  inner.blur();
  el.dispatchEvent(new Event('blur', { bubbles: true }));
  console.log("OK:", name, "→", val.substring(0, 40));
}
```

## A.3 Ordre d'exécution validé

1. Naviguer vers URL `/abis/listing/edit/product_details?sku=...&asin=...&vendorCode=...`
2. Attendre que `kat-textarea[name='item_name-0-value']` soit dans le DOM
3. Cliquer 4× sur "Ajouter plus" (bullets) avec délai 400ms entre chaque + attendre 2.5s
4. Exécuter `fillAndBlur()` sur tous les champs dans l'ordre
5. Mettre l'intercepteur XHR en place
6. Cliquer "Enregistrer et terminer" via shadow DOM
7. Vérifier `window._lastXHRResponse.status === "SUCCESS"`

## A.4 Gestion des bullets — `clickAddMore()`

```javascript
function clickAddMore() {
  var bulletGroup = null;
  document.querySelectorAll(".attributeGroup").forEach(function(g) {
    if (g.querySelector("kat-textarea[name*='bullet_point']")) bulletGroup = g;
  });
  var katLink = bulletGroup ? bulletGroup.querySelector("kat-link") : null;
  if (katLink && katLink.shadowRoot) {
    var anchor = katLink.shadowRoot.querySelector("a");
    if (anchor) { anchor.click(); return true; }
  }
  return false;
}
var clicks = 0;
function doClick() {
  if (clicks < 4) { clickAddMore(); clicks++; setTimeout(doClick, 400); }
}
doClick();
// Attendre 2.5s avant de remplir les bullets
```

## A.5 Intercepteur XHR + sauvegarde

```javascript
// Intercepteur XHR
var origOpen = XMLHttpRequest.prototype.open;
var origSend = XMLHttpRequest.prototype.send;
window._lastXHRResponse = null;
XMLHttpRequest.prototype.open = function(method, url) {
  this._url = url;
  return origOpen.apply(this, arguments);
};
XMLHttpRequest.prototype.send = function(body) {
  var self = this;
  this.addEventListener('load', function() {
    if (self._url && self._url.includes('abis')) {
      window._lastXHRResponse = { status: self.status, response: self.responseText };
    }
  });
  return origSend.apply(this, arguments);
};

// Sauvegarde
document.querySelectorAll("kat-button").forEach(function(btn) {
  if (btn.getAttribute('label') === "Enregistrer et terminer") {
    var ib = btn.shadowRoot && btn.shadowRoot.querySelector("button");
    if (ib) ib.click();
  }
});

// Vérification après 3s
setTimeout(function() {
  var r = window._lastXHRResponse;
  if (r && r.response.includes('"SUCCESS"')) {
    console.log("✅ SAUVEGARDE RÉUSSIE");
  } else {
    console.log("❌ ÉCHEC OU ERREUR:", r ? r.response.substring(0, 200) : "pas de réponse");
  }
}, 3000);
```

## A.6 Limites connues

| Limite | Détail |
|---|---|
| `generic_keyword` trop long | Amazon limite à ~250 chars — modale d'erreur bloquante si dépassé |
| Modale d'erreur | Cliquer `kat-button[label='OK']` via shadow DOM pour naviguer vers le champ en erreur |
| `execCommand` déprécié | Toujours fonctionnel sur Chrome — surveiller les mises à jour navigateur |
| Sélecteurs Katal fragiles | Amazon peut changer l'interface VC — tester après chaque mise à jour VC |

---

# PARTIE B — PLAN v3.4.24

## B.1 Refonte `buildVCModifyPrompt` — bloc JS complet

**Fichier :** `src/seo.js`

Remplacer le bloc JS `(function() {...})()` dans `lines` par le nouveau bloc intégrant :

### Nouveau bloc JS à générer dans le script

```javascript
(function() {
  // ── INTERCEPTEUR XHR ──────────────────────────────────────
  var origOpen = XMLHttpRequest.prototype.open;
  var origSend = XMLHttpRequest.prototype.send;
  window._lastXHRResponse = null;
  XMLHttpRequest.prototype.open = function(m, url) {
    this._url = url; return origOpen.apply(this, arguments);
  };
  XMLHttpRequest.prototype.send = function(body) {
    var self = this;
    this.addEventListener('load', function() {
      if (self._url && self._url.includes('abis'))
        window._lastXHRResponse = { status: self.status, response: self.responseText };
    });
    return origSend.apply(this, arguments);
  };

  // ── FILL AND BLUR ─────────────────────────────────────────
  function fillAndBlur(name, val) {
    var el = document.querySelector("kat-textarea[name='" + name + "'], kat-input[name='" + name + "']");
    if (!el) { console.warn("NON TROUVÉ:", name); return; }
    var inner = el.shadowRoot
      ? (el.shadowRoot.querySelector('textarea') || el.shadowRoot.querySelector('input'))
      : null;
    if (!inner) { console.warn("INNER NON TROUVÉ:", name); return; }
    inner.focus(); inner.click();
    document.execCommand('selectAll');
    document.execCommand('delete');
    document.execCommand('insertText', false, val);
    inner.blur();
    el.dispatchEvent(new Event('blur', { bubbles: true }));
    console.log("OK:", name, "→", (val||"").substring(0, 40));
  }

  // ── AJOUTER BULLETS 1-4 ───────────────────────────────────
  function clickAddMore() {
    var bulletGroup = null;
    document.querySelectorAll(".attributeGroup").forEach(function(g) {
      if (g.querySelector("kat-textarea[name*='bullet_point']")) bulletGroup = g;
    });
    var katLink = bulletGroup ? bulletGroup.querySelector("kat-link") : null;
    if (katLink && katLink.shadowRoot) {
      var anchor = katLink.shadowRoot.querySelector("a");
      if (anchor) { anchor.click(); return true; }
    }
    return false;
  }

  // ── ERREURS DE VALIDATION ─────────────────────────────────
  function checkErrors() {
    var errors = [];
    document.querySelectorAll("[class*='error']").forEach(function(el) {
      var t = el.textContent.trim();
      if (t) errors.push(t);
    });
    return errors;
  }

  // ── SAUVEGARDE ────────────────────────────────────────────
  function save() {
    document.querySelectorAll("kat-button").forEach(function(btn) {
      if (btn.getAttribute('label') === "Enregistrer et terminer") {
        var ib = btn.shadowRoot && btn.shadowRoot.querySelector("button");
        if (ib) ib.click();
      }
    });
    setTimeout(function() {
      var r = window._lastXHRResponse;
      if (r && r.response.includes('"SUCCESS"')) {
        console.log("✅ SAUVEGARDE RÉUSSIE — vendor code traité.");
      } else {
        console.log("❌ ÉCHEC:", r ? r.response.substring(0, 200) : "pas de réponse XHR");
      }
    }, 3000);
  }

  // ── SÉQUENCE PRINCIPALE ───────────────────────────────────
  // Étape 1 : ajouter bullets 1-4
  var clicks = 0;
  function doClick() {
    if (clicks < 4) { clickAddMore(); clicks++; setTimeout(doClick, 400); }
    else setTimeout(fillAll, 2500);
  }
  doClick();

  // Étape 2 : remplir tous les champs
  function fillAll() {
    fillAndBlur("item_name-0-value", [TITRE]);
    fillAndBlur("bullet_point-0-value", [BULLET_0]);
    fillAndBlur("bullet_point-1-value", [BULLET_1]);
    fillAndBlur("bullet_point-2-value", [BULLET_2]);
    fillAndBlur("bullet_point-3-value", [BULLET_3]);
    fillAndBlur("bullet_point-4-value", [BULLET_4]);
    fillAndBlur("rtip_product_description-0-value", [DESCRIPTION]);
    fillAndBlur("generic_keyword-0-value", [BACKEND_KW]);

    // Étape 3 : vérifier erreurs
    setTimeout(function() {
      var errors = checkErrors();
      if (errors.length) {
        console.log("⚠️ ERREURS DÉTECTÉES — ne pas sauvegarder :");
        errors.forEach(function(e) { console.log("→", e); });
        console.log("Signale ces erreurs à Fred.");
      } else {
        console.log("✅ Aucune erreur — lancement sauvegarde...");
        save();
      }
    }, 1000);
  }
})();
```

**Note :** `[TITRE]`, `[BULLET_N]`, `[DESCRIPTION]`, `[BACKEND_KW]` sont remplacés par `JSON.stringify(valeur)` au moment de la génération dans `buildVCModifyPrompt`.

**Le script ne demande plus le GO de Fred avant de sauvegarder** — il vérifie les erreurs et sauvegarde automatiquement si aucune erreur.

## B.2 Suppressions dans `lines`

Supprimer :
- L'étape `2c` (lire console + attendre GO)
- L'étape `2d` (après GO cliquer)
- Les références à `fillField` (remplacé par `fillAndBlur`)

Garder :
- Étape 1 catalogue
- Étape 2a navigation URL
- Étape 2b exécution JS (nouveau bloc)
- Étape 3 bilan final

---

# PARTIE C — BUGS À TRAITER (session suivante)

## C.1 Erreurs de validation Amazon (P0)

Les 2-3 principales erreurs qui bloquent la sauvegarde :

| Erreur connue | Cause | Fix dans le script |
|---|---|---|
| `generic_keyword` trop long | > ~250 chars | Tronquer à 249 chars dans `buildVCModifyPrompt` |
| Modale d'erreur bloquante | Champ invalide | Détecter + cliquer `kat-button[label='OK']` via shadow DOM |
| Champ obligatoire manquant | Ex: "Emballage" absent | Détecter via `checkErrors()` + signaler à Fred |

**Gestion modale à ajouter dans `checkErrors()` :**
```javascript
// Détecter et fermer modale d'erreur
var modal = document.querySelector("kat-modal");
if (modal && modal.visible) {
  var okBtn = modal.shadowRoot && modal.shadowRoot.querySelector("kat-button[label='OK']");
  if (okBtn) {
    var inner = okBtn.shadowRoot && okBtn.shadowRoot.querySelector("button");
    if (inner) inner.click();
  }
}
```

## C.2 Backend keywords — non-conformité (P0)

**Problème :** les keywords générés ne respectent pas le cahier des charges ni sur le fond ni sur la forme.

**Règles à appliquer (CDC SEO — section 2) :**
- Séparés par espaces uniquement (pas de virgules, pas de ponctuation)
- Pas de répétition des mots du titre ou des bullets
- Structure 5 blocs : synonymes → usages → contextes → matériaux/compatibilités → longue traîne
- 40-60 mots maximum
- Pas de claims, pas de marques concurrentes, pas d'ASIN
- **Limite stricte Amazon : 249 caractères maximum** (tronquer si dépassé)

**Fix dans `buildSEOPrompt` (`src/seo.js`) :**
Vérifier que la directive `BACKEND_KEYWORDS` impose explicitement la limite de 249 caractères.

**Fix dans `buildVCModifyPrompt` (`src/seo.js`) :**
```javascript
var backendKW = (ficheData.backendKW || fiche.backendKW || '')
  .replace(/\*\*/g, '')
  .replace(/,/g, ' ')        // supprimer virgules
  .replace(/\s+/g, ' ')      // normaliser espaces
  .trim()
  .substring(0, 249);        // limite stricte Amazon
```

---

# PARTIE D — MAPPING DOM VENDOR CENTRAL (référence)

## D.1 Sélecteurs validés (nouvelle interface VC — mai 2026)

| Champ | Sélecteur kat-* | Type | Notes |
|---|---|---|---|
| Titre | `kat-textarea[name="item_name-0-value"]` | kat-textarea | Max ~200 chars |
| Bullet 1 | `kat-textarea[name="bullet_point-0-value"]` | kat-textarea | Présent au chargement |
| Bullet 2-5 | `kat-textarea[name="bullet_point-1/2/3/4-value"]` | kat-textarea | **Requiert clic "Ajouter plus" x4** |
| Description | `kat-textarea[name="rtip_product_description-0-value"]` | kat-textarea | HTML accepté |
| Backend KW | `kat-input[name="generic_keyword-0-value"]` | kat-input | Max 249 chars strict |
| Marque | `kat-input[name="brand-0-value"]` | kat-input | Verrouillé |

## D.2 Méthode de remplissage validée

```javascript
// ✅ SEULE MÉTHODE QUI FONCTIONNE
function fillAndBlur(name, val) {
  var el = document.querySelector("kat-textarea[name='" + name + "'], kat-input[name='" + name + "']");
  var inner = el.shadowRoot
    ? (el.shadowRoot.querySelector('textarea') || el.shadowRoot.querySelector('input'))
    : null;
  inner.focus(); inner.click();
  document.execCommand('selectAll');
  document.execCommand('delete');
  document.execCommand('insertText', false, val);
  inner.blur();
  el.dispatchEvent(new Event('blur', { bubbles: true }));
}

// ❌ MÉTHODES QUI NE FONCTIONNENT PAS
// - el.value = val                          (React ignore)
// - nativeInputValueSetter.call(el, val)    (React ignore)
// - dispatchEvent('input'/'change') seul    (React ignore)
```

## D.3 Sauvegarde

```javascript
// Bouton "Enregistrer et terminer" via shadow DOM
document.querySelectorAll("kat-button").forEach(function(btn) {
  if (btn.getAttribute('label') === "Enregistrer et terminer") {
    var ib = btn.shadowRoot && btn.shadowRoot.querySelector("button");
    if (ib) ib.click();
  }
});
```

---

# PARTIE E — ÉTAT INFRASTRUCTURE

## E.1 Versions

| Env | Version | URL |
|---|---|---|
| Production | v3.4.18 | https://amazon.foliow.app |
| Staging | v3.4.23 | https://d9xny9istvl53.cloudfront.net |
| Preprod | v3.4.23 | https://preprod.amazon.foliow.app |

**À faire avant merge main :** merger v3.4.23 → main (backend keywords fix + regex description)

## E.2 AWS

| Ressource | Valeur |
|---|---|
| S3 prod | `amazon-pilot-foliow` |
| CloudFront prod | `E3ERL241475BJI` |
| S3 staging | `amazon-pilot-recette` |
| CloudFront staging | `EVQ30COFUNGA7` |
| S3 preprod | `amazon-pilot-preprod` |
| CloudFront preprod | `E3CODYJ437XKU5` |
| Lambda imports | `https://hue3u3z5ghbi4tcj2lxqewk4ua0nrbyx.lambda-url.eu-west-3.on.aws` |
| Lambda API prod | `https://konuaxmdxjnzcuw2etjqwczrla0xycvt.lambda-url.eu-west-3.on.aws` |
| Cognito | `eu-west-3_8P9UzCONx` / `5nnllolhnc3572800bvce94682` |
| Région | `eu-west-3` |

---

# PARTIE F — RÈGLES SESSION (invariants)

1. Fred valide, Claude Code exécute. Exposer le plan avant tout code.
2. Tout commit → staging → validation Fred → merge main. Sans exception.
3. `node --check` obligatoire avant livraison.
4. Livrable nommé `amazon-pilot-vX.Y.Z.html`.
5. Ne jamais inventer de spec produit dans le prompt SEO.
6. Consignes Claude Code : inline si court.
7. Autocritique en fin de session.
8. Avant toute refonte UX : grep exhaustif de tous les appelants dans `src/`.
9. Après chaque push staging : `git -C C:\AmazonPilot\repo pull origin staging`.
10. Staging → preprod → validation → merge main. Jamais de commit direct sur main.
11. Localisation fonctions clés : `buildSEOPrompt`, `parseSEOResponse`, `renderAgentVC`, `buildVCModifyPrompt` → `src/seo.js` | `runSEOFiche`, `callAPI`, `goAgentVC`, `renderSEOSection` → `src/core.js`

---

# PARTIE G — AUTOCRITIQUE SESSION

1. **6 itérations pour trouver `fillAndBlur`** — j'aurais dû partir directement de l'hypothèse "React contrôlé" et tester `execCommand` en premier plutôt que de tester successivement toutes les méthodes DOM.

2. **Option B (extension Chrome) proposée trop tôt** — j'ai déclaré une limite structurelle alors que la solution existait. C'est Fred qui l'a trouvée, pas moi.

3. **`generic_keyword` trop long** — la limite de 249 chars Amazon était dans le CDC depuis le début. J'aurais dû l'intégrer dans `buildVCModifyPrompt` dès v3.4.20.

---

**FIN RÉCAP v3.4.24 — 8 mai 2026**
