# DIFF contexte Orchestrateur → V0.13

**Date:** June 18, 2026
**De:** Orchestrateur produit (contenu) — à appliquer par Claude Code sur `Claude_Orchestrateur_Context.md` (repo)
**Base:** V0.12 (15 juin 2026). Procédure : diff structurée, Claude Code applique + sync.

---

## MODIF 1 — En-tête (ligne 2)

Remplacer :
`**Version :** V0.12 — 15 juin 2026`
par :
`**Version :** V0.13 — 18 juin 2026`

---

## MODIF 2 — Insérer ce bloc en tête de l'historique de versions (juste après « **Historique de versions :** », avant la ligne V0.12)

- **V0.13 (18 juin 2026) — Cycle v3.8 : moteur tag suspect livré + CONCEPTION COMPLÈTE du module Buy Box (processus 3 agents) + refonte UI Détecter. (1) HOTFIX MODÈLES IA v3.7.12 (prod) : `claude-sonnet-4-20250514` retiré par Anthropic → analyses cassées. Migration vers 4 modèles NON DATÉS (`sonnet-4-6`, `opus-4-6`, `opus-4-8`, `haiku-4-5`). CONVENTION GRAVÉE : version-mineure, jamais de suffixe date, jamais `latest`. Routage FIGÉ par feature (gouvernance produit ; sélecteur modèle retiré de l'UI client). Découverte clé : la Lambda IA était une WHITELIST (pas un proxy transparent) avec fallback sur modèle retiré → un déploiement app-only aurait cassé ; clone Lambda obligatoire. 2 bypass hardcodés rapatriés (`runApprosIA`, `initAIDiagnostic`). Tarifs sourcés /claude-api (Opus 15/75→5/25). (2) FIX ERP v3.7.13 (prod) — parser `erp_stock`, cycle v3.7 clos. (3) MOTEUR TAG SUSPECT v3.8.0/v3.8.1 (preprod) : `computeSuspectTags` fonction pure, `SUSPECT_CONFIG` centralisée, insertion `mergeImportData` ~l.4370. Double composante (zéro hebdo ≥3 sem réactif + chute/hausse mensuelle glissante 8 sem). Renforçateur dormant = ratio `aged90SellableUnits/sellableUnits ≥ 50 %` (`AGED90_RATIO_CRIT`, paramétrable ; distribution bimodale 0 %/100 % → seuil robuste). PAS de renforçateur rupture (sellable=0+chute = BB-1, niveau 2). `caExposed` = `annualData['2025'].ventes.asins[].revenue` réconcilié Fab/Appro (fallback 2024 puis somme 8-sem ; 92 % sur 2025). **RECALCUL PROPRE (référence) : 86 crit / 90 warn / 57 opp / 233 taggés** (les 233 antérieurs dataient de v3.8.0 SANS renforçateur → 23 warn promus crit). Split crit : 67 zero_hebdo + 19 chute+renforçateur ; 60 ASINs renforcés. (4) SIGNAL — CORRECTION V0.12 : le §10 de V0.12 (« ratio FO/Glance ») est CADUC. Confirmé et figé (cohérent avec V0.12 §6) : signal = **dynamique des FO Page Views**, PAS un ratio (FO ≈ Glance pour un 1P). Reste backlog : faire reformuler le « taux d'exposition = FO/Glance » du doc méthodo V3 par GPT. (5) MODULE BUY BOX = PROCESSUS VIVANT À 3 AGENTS (Détecter → Diagnostiquer → Agir) sur la chaîne de confiance (stock possédé ≠ sellable ≠ trusted ≠ exposé ≠ Buy Box). Décision (c) ACTÉE : page `Processus` en PORTE D'ENTRÉE + FIL 3 agents PERSISTANT dans chaque écran. Chaque écran = « image figée d'un processus vivant ». Maillons : Détecter=Trafic/FO, Diagnostiquer=Stock→BB, Agir=Offre Retail. (6) DÉTECTER = ÉCRAN UNIFIÉ, 2 LENTILLES : Exposition (`suspectTag`, crit/warn/opp, amont) + Buy Box perdue (`retailPct`, Perdue/Compromise/Fragile/Récupérée, perte actée). Entonnoir + fil communs. BADGE onglet = `retailPct` (perte actée prime). `calcBuyBoxAlerts` CONSERVÉ (moteur retailPct, alimente lentille+badge). (7) DIAGNOSTIQUER = AGENT BB RECOVERY SEUL. Carnet Phase 2 (11 hypothèses) SUPPRIMÉ — `buyboxCases` vidé preprod (Cogex+Gers=0) + prod. Plomberie de dossier RÉUTILISÉE (audit : 7 fonctions/11 réutilisables telles quelles — CRUD/journal/gate conclusion/close fonctionnent par `id`, schéma `buyboxCases` INCHANGÉ ; à réécrire : `buyboxAutoEvaluateHypotheses` + `renderBuyBoxCase`). Code carnet en sommeil mais ROUTAGE UTILISATEUR COUPÉ (« Lancer l'Agent BB » → stub, jamais `renderBuyBoxCase`). (8) CAPTURE PAGE AMAZON = 2 COLLAGES guidés (TESTÉS en direct sur cas réels) : copie 1 (éditoriale, déjà gérée via textarea→IA façon Agent SEO) + copie 2 (BLOC D'ACHAT : prix featured, offres, « Vendu par »/« Expédié par ») OBLIGATOIRE (Règle 3 : qui détient la Buy Box), y compris MONO-VENDEUR. CORRECTION : il n'y a PAS de « parseur SEO » réutilisable — le collage SEO est un textarea brut envoyé à l'IA. Donc capture BB = PATTERN IA (`parseBuyBoxCapture(text,lang)`, Option B), PAS un parseur regex. (9) RÈGLE DE LECTURE BLOC D'ACHAT (autorité GPT, validée Fred) — parser « Vendu par » ET « Expédié par » SÉPARÉMENT : Vendu+expédié Amazon = 1P (Vendor Central) sain ; Vendu par X + expédié Amazon = 3P FBA (concurrent aligné Prime, dur à déloger) ; Vendu+expédié X = 3P dropshipping/MFN (levier promesse, porte 3 / famille H). (10) CONCORDANCE processus↔app produite + CORRIGÉE par audit Claude Code : ASN/DESADV (`parseDeliveryDefectsCSV`) et CARP/ISA/Appointment (`parseAppointmentsCSV` 17 champs) sont INGÉRÉS (concordance initiale ✗ → ✓) ; Phase 2 était ACTIVE (pas « obsolète ») ; UI suspects lit bien `a.suspectTag` réel. SEUL VRAI TROU confirmé = capture page Amazon (disponibilité/détenteur Buy Box). (11) UI DÉTECTER v3.8.3 (preprod) : `renderSuspectsBoard`→`renderDetecterBoard`, `_buyboxLens` global, fil 3 agents + entonnoir + sélecteur lentille, harmonisée au style app (abandon monospace maquette). NARRATION VALIDÉE visuellement par Fred (« beaucoup plus compréhensible »). (12) DÉCISION MERGE : v3.8.x reste PREPROD — merge prod GROUPÉ avec l'Agent BB Recovery (« à minima l'agent » : un Détecter qui mène à un stub n'a pas de valeur en prod). (13) CHIFFRES COGEX RÉALISTES (autorité Fred) : 1231 ASINs « Buy Box perdue »/1299 = compte PATHOLOGIQUE réel (3 ans de promesse d'exécution non tenue), PAS un artefact de compteur. Caveat compte pathologique maintenu (seuils à recalibrer sur compte sain). Backlog : lentille BB perdue à hiérarchiser (récupérable vs structurellement mort — 0 %/listing supprimé). (14) LEÇONS DE MÉTHODE (autocritique) : (a) ÉNIGME DES CHIFFRES INVENTÉE — j'ai affirmé un écart UI (104 vs 233…) issu de la MAQUETTE prise pour le rendu preprod ; appliquer à MES PROPRES observations la rigueur de sourcing que j'exige de Claude Code (présence ≠ flux, vérifier la SOURCE) ; (b) CONCORDANCE TROP PESSIMISTE — ✗ par défaut sur l'inconnu (corrigé 3/4 par l'audit) ; « inconnu » ≠ « absent » ; (c) ERREUR D'ORCHESTRATION — maquette suspects validée EN SILO sur le mauvais critère (affichage des données vs narration du processus) alors que la maquette Processus 3 agents existait → UI muette ; PARTIR DU PROCESSUS, pas de l'écran isolé ; (d) RÈGLE N°1 ENFREINTE — nettoyage `buyboxCases` prod exécuté par Fred en CONSOLE faute de voie technique ; j'aurais dû refuser le brief qui mettait Fred à la console → créer une VOIE PROPRE de maintenance prod (backlog) ; (e) « nouveau chantier Fab/Appro » annoncé à tort (déjà `sourcingOnly` depuis avril). CE QUI A TENU : refus de merger sans valeur réelle ; STOP sur sommation `a.history` (granularité hebdo/annuel) ; acceptation du recadrage Fred « revenons au basic » ; ne pas trancher le fond VC à la place de GPT/Fred (familles, règles de lecture, réalisme Cogex) ; clone Lambda obligatoire. (15) POINT DE REPRISE = cadrage AGENT BB RECOVERY (6 portes → BB-1→12 → 16 familles A→P, capture 2 copies, dossier réutilisé) — mérite son propre cadrage AVANT le brief de code.**

---

## MODIF 3 — Statut prod / roadmap (section à localiser par Claude Code)

- **Statut prod** : v3.7.13 (était v3.7.11). **Preprod** : v3.8.3 (moteur tag suspect + UI Détecter, validés, non mergés prod).
- **Roadmap** : v3.8 phase CONCEPTION → désormais EN COURS DE LIVRAISON. Reste : Agent BB Recovery (niveau 2), intégration page Processus, Agent Communication (v3.9).

---

## MODIF 4 — Règles candidates à ajouter (numéro à confirmer par Claude Code — suite de la dernière règle existante)

- **Règle [n] — Partir du processus, pas de l'écran.** Toute maquette/refonte d'écran appartenant à un parcours multi-étapes se valide sur sa NARRATION dans le parcours (« raconte-t-elle le processus ? »), pas sur l'affichage des données. Cadre global d'abord, écran isolé ensuite.
- **Règle [n+1] — Voie propre de maintenance prod.** Aucune opération de données en prod via console Fred (règle n°1). Toute maintenance prod passe par une voie technique (Claude Code / CloudShell / Chrome auto) + pipeline. « Domaine bloqué » n'autorise pas à transformer Fred en console.
- **Règle [n+2] — Rigueur de sourcing réflexive.** Avant d'affirmer un écart/anomalie, vérifier la SOURCE de l'observation (maquette vs preprod vs prod). La règle « vérifier les faits avant d'imputer » s'applique à l'Orchestrateur lui-même.

---

[Agent Orchestrateur] — Source : session 18/06 + V0.12 repo — Confiance : haute ; MODIF 3/4 à localiser/numéroter par Claude Code ; correction explicite du §10 V0.12 (signal = dynamique FO, pas ratio)
