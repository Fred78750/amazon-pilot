# RÉCAP & PLAN D'ACTION v3.4.16
**Date :** 7 mai 2026
**Production (main) :** v3.2.24 — https://amazon.foliow.app
**Staging :** v3.4.16 — https://d9xny9istvl53.cloudfront.net
**Preprod :** v3.4.16 — https://preprod.amazon.foliow.app

---

# PARTIE A — RÉCAP SESSION DU 7 MAI 2026

## A.1 Contexte de démarrage

| Élément | État |
|---|---|
| Staging au démarrage | v3.4.2 |
| Objectif initial | Correctifs Agent SEO+VC — prompts description HTML + wizard |
| Livraisons session | v3.4.3 → v3.4.16 (14 versions) |

## A.2 Infrastructure ajoutée

| Ressource | Valeur |
|---|---|
| Bucket S3 preprod | `amazon-pilot-preprod` (eu-west-3) |
| CloudFront preprod | `E3CODYJ437XKU5` |
| URL preprod | `https://preprod.amazon.foliow.app` |
| OAC preprod | `E2F4WDEVWY6L4Y` |
| Branche CI | `preprod` → `deploy-preprod.yml` |
| CNAME Gandi | `preprod.amazon` → `dyhpahzg19h3i.cloudfront.net` |

**Règle ajoutée au protocole :** après chaque push staging → `git -C C:\AmazonPilot\repo pull origin staging`

## A.3 Fixes livrés (v3.4.3 → v3.4.16)

| Version | Fichier | Fix |
|---|---|---|
| v3.4.3 | `src/seo.js` | `parseSEOResponse` : strip `**` sur `description` + directive DESCRIPTION HTML stricte dans `buildSEOPrompt` |
| v3.4.3 | `src/seo.js` | Directive BACKEND_KEYWORDS restructurée en 5 blocs mentaux + INTERDIT étendu |
| v3.4.4 | `src/seo.js` | Strip `**` sur 4 champs synthèse (`positionnement`, `leviers`, `erreurs`, `opportunite`) |
| v3.4.4 | `src/core.js` | Guard `apiKey` dans `runSEOFiche` → `__ERR_NOKEY__` propre |
| v3.4.4 | `src/seo.js` | Refonte `renderAgentVC` — wizard 5 étapes avec `avcStepWrap`, helpers `avc*` |
| v3.4.5 | `src/core.js` | `renderOnboarding` : `c.` → `nc.` sur 4 lignes (wizStep 3, bloc PO) |
| v3.4.6 | `src/seo.js` | Champ SKU : `onchange` → `oninput` (frappe tronquée au 1er caractère) |
| v3.4.6 | `src/seo.js` | Bouton "📤 Script VC →" ajouté dans branche `ficheReady` étape 4 |
| v3.4.7 | `src/seo.js` | `go('agentseo')` → `go('seo')` sur 2 occurrences (screen inexistant) |
| v3.4.8 | `src/seo.js` | `backendKW` : chemin plat → `fiche.backendKW` (per-market) |
| v3.4.8 | `src/seo.js` | `seoLaunchModify` : route vers `goAgentVC` si `vendorCodes.length > 1` |
| v3.4.9 | `src/seo.js` | `seoLaunchModify` → toujours `goAgentVC` (suppression bifurcation mono/multi-VC) |
| v3.4.9 | `src/seo.js` | Suppression `showVCConfirmModal` + `_doVCCopy` (dead code) |
| v3.4.10 | `src/seo.js` | "Voir fiche complète" : `go('seo')` → `selectedAsin=asin; go('asins')` |
| v3.4.10 | `src/seo.js` | Boutons "SEO"+"VC" liste "À surveiller" → fusionnés en "🚀 Optimiser" → `goAgentVC` |
| v3.4.11 | `src/core.js` | Bouton "📤 Optimiser + Publier VC" : `btn-p` → `btn-or` (orange) sur 2 occurrences |
| v3.4.12 | `src/seo.js` | `seoSearchGo` : `openSEODrawer` → `goAgentVC` (unification points d'entrée) |
| v3.4.13 | `src/core.js` | `runSEOFiche` : `render()` après `refreshSEODrawer()` (UI bloquée après génération) |
| v3.4.14 | `src/seo.js` | `avcCopyScript` : fallback `ficheOptimisee` si `seoResults` vide après rechargement |
| v3.4.15 | `src/seo.js` | `avcCopyScript` : `navigator.clipboard` → `execCommand` pur (clipboard silencieux) |
| v3.4.16 | `src/seo.js` | `avcCopyScript` : `JSON.stringify(vc)` → `'\'' + vc + '\''` sur 3 lignes (onclick cassé) |

## A.4 Unification des points d'entrée Agent VC (résultat final)

| Point d'entrée | Avant | Après v3.4.12 |
|---|---|---|
| Champ ASIN + "✍️ Générer" | `openSEODrawer` | `goAgentVC` ✅ |
| Liste "À surveiller" — "🚀 Optimiser" | `goAgentVC` | `goAgentVC` ✅ |
| Drawer footer — "📤 Modifier VC" | `seoLaunchModify` → `goAgentVC` | `goAgentVC` ✅ |
| Vue détail ASIN — "📤 Optimiser + Publier VC" | `goAgentVC` | `goAgentVC` ✅ |

## A.5 Règle de session ajoutée

**Avant toute refonte UX impliquant une fonction centrale :** cartographier exhaustivement tous les appelants (`grep` sur le nom de fonction dans tout `src/`) avant d'écrire une ligne.

---

# PARTIE B — BUGS ET DETTES RESTANTS

## B.1 Bug en cours (P0 — bloquant étape 5)

**Nature :** l'étape suivante après "Copier le script" bloque — nature exacte non encore diagnostiquée en fin de session.

**À diagnostiquer en début de session suivante :** ouvrir preprod, arriver étape 5, cliquer "Copier le script" (confirmé ✅ v3.4.16), puis observer ce qui bloque à l'étape suivante.

## B.2 Tests pré-merge main non terminés

| Test | Statut | Remarque |
|---|---|---|
| Description HTML sans `**` | ⚠️ N/A | Nécessite clé API + régénération |
| Synthèse sans `**` | ⚠️ N/A | Nécessite clé API + régénération |
| Guard `__ERR_NOKEY__` | ✅ PASS | v3.4.4 |
| SKU guard étape 3 | ✅ PASS | v3.4.6 |
| Multi-VC étape 5 (COGEX + 3J6MN) | ❌ FAIL | `c.vendorCodes` non configuré en preprod + `buildVCModifyPrompt` ignore param `vc` |
| URL COGEX + SKU | ✅ PASS | Format correct |
| URL 3J6MN + SKU 643416 | ❌ FAIL | Dépend du fix multi-VC |
| "Voir fiche complète" → Agent SEO | ✅ PASS | v3.4.10 |
| Copie script clipboard | ✅ PASS | v3.4.16 |

## B.3 Fix multi-VC non appliqué (planifié v3.4.9, jamais exécuté)

**`buildVCModifyPrompt`** : accepter `vc` comme 6e paramètre pour l'URL (sinon URL 3J6MN générée avec `vendorCode=COGEX`).

```javascript
// Signature actuelle
function buildVCModifyPrompt(asin, market, fiche, c, sku)
// Signature cible
function buildVCModifyPrompt(asin, market, fiche, c, sku, vc)
// Dans le corps — URL
'&vendorCode=' + (vc || c.vendorCode || '') +
```

**`avcCopyScript`** : passer `vc` à `buildVCModifyPrompt`.

---

# PARTIE C — RÈGLES MÉTIER (invariants)

## C.1 Multi-vendor codes Cogex

| Vendor Code | Rôle | SKU exemple |
|---|---|---|
| COGEX | VC principal | `B07DPCH7XC` (= ASIN) |
| 3J6MN | VC secondaire | `643416` |

Règle : modifier **tous** les VC avec le même contenu. Pas de VC maître.

## C.2 SEO — Règles de référence (gravées dans `buildSEOPrompt`)

- Titre : tirets `-` uniquement, pas de `|` ni `/`
- Référence interne immédiatement après la marque
- 5 bullets max, 1 icône par bullet en premier
- Backend KW : 250 bytes max, espaces uniquement, 5 blocs mentaux
- Description HTML : `<p>` `<strong>` `<ul>` `<li>` uniquement, 400-800 chars
- **Règle absolue :** specs uniquement issues de `seoFetchFiche` — jamais inventées

## C.3 Localisation des fonctions clés (`src/`)

| Fonction | Fichier |
|---|---|
| `buildSEOPrompt` | `src/seo.js` |
| `parseSEOResponse` | `src/seo.js` |
| `renderAgentVC` + helpers `avc*` | `src/seo.js` |
| `seoLaunchModify`, `seoSearchGo` | `src/seo.js` |
| `buildVCModifyPrompt` | `src/seo.js` |
| `runSEOFiche` | `src/core.js` |
| `callAPI`, `askClaude` | `src/core.js` |
| `goAgentVC` | `src/core.js` |
| `renderSEOSection` | `src/core.js` |

---

# PARTIE D — PLAN SESSION SUIVANTE

## D.1 Priorités

| Priorité | Action | Fichier |
|---|---|---|
| P0 | Diagnostiquer et corriger le bug étape 5 post-copie | `src/seo.js` |
| P0 | Fix `buildVCModifyPrompt` : paramètre `vc` pour URL multi-VC | `src/seo.js` |
| P1 | Configurer `vendorCodes: ['COGEX', '3J6MN']` en preprod | Fiche client preprod |
| P1 | Tests 1+2 : description HTML sans `**` + synthèse sans `**` | preprod avec clé API |
| P1 | Tests 5+7 : multi-VC (COGEX + 3J6MN) | preprod |
| P2 | Merge main si tous tests ✅ | `main` |

## D.2 Règles session (invariants)

1. Fred valide, Claude Code exécute. Exposer le plan avant tout code.
2. Tout commit → staging → validation Fred → merge main. Sans exception.
3. `node --check` obligatoire avant livraison.
4. Livrable nommé `amazon-pilot-vX.Y.Z.html`.
5. Ne jamais inventer de spec produit dans le prompt SEO.
6. Consignes Claude Code : inline si court, MD téléchargeable si dense.
7. Autocritique en fin de session.
8. Avant toute refonte UX : `grep` exhaustif de tous les appelants dans `src/`.
9. Après chaque push staging : `git -C C:\AmazonPilot\repo pull origin staging`.

---

# PARTIE E — ÉTAT INFRASTRUCTURE

## E.1 Versions

| Env | Version | URL |
|---|---|---|
| Production | v3.2.24 | https://amazon.foliow.app |
| Staging | v3.4.16 | https://d9xny9istvl53.cloudfront.net |
| Preprod | v3.4.16 | https://preprod.amazon.foliow.app |

## E.2 AWS

| Ressource | Valeur |
|---|---|
| S3 prod | `amazon-pilot-foliow` |
| CloudFront prod | `E3ERL241475BJI` |
| S3 staging | `amazon-pilot-recette` |
| CloudFront staging | `EVQ30COFUNGA7` |
| S3 preprod | `amazon-pilot-preprod` |
| CloudFront preprod | `E3CODYJ437XKU5` |
| Lambda imports | `https://hue3u3z5ghbi4tcj2lxqewk4ua0nrbyx.lambda-url.eu-west-3.on.aws` |
| Lambda API prod | `https://konuaxmdxjnzcuw2etjqwczrla0xycvt.lambda-url.eu-west-3.on.aws` |
| Cognito | `eu-west-3_8P9UzCONx` / `5nnllolhnc3572800bvce94682` |
| Région | `eu-west-3` |

---

**FIN RÉCAP v3.4.16 — 7 mai 2026**
