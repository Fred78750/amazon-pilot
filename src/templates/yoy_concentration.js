// Amazon Pilot — YoY Template : Concentration du CA (section 3)
// Implémentation complète : CP3

/**
 * tplConcentration — Template interprétatif section "Concentration du chiffre d'affaires"
 * @param {object} dims  — Résultats dimension 9
 * @param {string} sign  — 'negative' | 'positive' | 'stable'
 * @returns {string}     — Paragraphe HTML
 */
function tplConcentration(dims, sign) {
  // CP3 — placeholder
  return `<p style="color:var(--tx2);font-style:italic">Analyse de la concentration en cours de rédaction (v3.6.5-CP3).</p>`;
}
