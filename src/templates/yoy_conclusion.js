// Amazon Pilot — YoY Template : Conclusion (section finale)
// Implémentation complète : CP3

/**
 * tplConclusion — Template interprétatif section "Conclusion"
 * @param {object} dims  — Résultats toutes dimensions
 * @param {string} sign  — 'negative' | 'positive' | 'stable'
 * @returns {string}     — Paragraphe HTML
 */
function tplConclusion(dims, sign) {
  // CP3 — placeholder
  return `<p style="color:var(--tx2);font-style:italic">Conclusion en cours de rédaction (v3.6.5-CP3).</p>`;
}
