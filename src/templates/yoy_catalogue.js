// Amazon Pilot — YoY Template : Mouvement du catalogue (section 2)
// Implémentation complète : CP3

/**
 * tplCatalogue — Template interprétatif section "Mouvement du catalogue"
 * @param {object} dims  — Résultats dimensions 7-8
 * @param {string} sign  — 'negative' | 'positive' | 'stable'
 * @returns {string}     — Paragraphe HTML
 */
function tplCatalogue(dims, sign) {
  // CP3 — placeholder
  return `<p style="color:var(--tx2);font-style:italic">Analyse du mouvement catalogue en cours de rédaction (v3.6.5-CP3).</p>`;
}
