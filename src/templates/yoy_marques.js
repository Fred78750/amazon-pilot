// Amazon Pilot — YoY Template : Ventilation par marque (section 4)
// Implémentation complète : CP3

/**
 * tplMarques — Template interprétatif section "Ventilation par marque"
 * @param {object} dims  — Résultats dimension 10
 * @param {string} sign  — 'negative' | 'positive' | 'stable'
 * @returns {string}     — Paragraphe HTML
 */
function tplMarques(dims, sign) {
  // CP3 — placeholder
  return `<p style="color:var(--tx2);font-style:italic">Analyse par marque en cours de rédaction (v3.6.5-CP3).</p>`;
}
