// Amazon Pilot — YoY Template : Top mouvements ASIN (section 5)
// Implémentation complète : CP3

/**
 * tplTopMouvements — Template interprétatif section "Top mouvements ASIN"
 * @param {object} dims  — Résultats dimension 11
 * @param {string} sign  — 'negative' | 'positive' | 'stable'
 * @returns {string}     — Paragraphe HTML
 */
function tplTopMouvements(dims, sign) {
  // CP3 — placeholder
  return `<p style="color:var(--tx2);font-style:italic">Analyse des mouvements ASIN en cours de rédaction (v3.6.5-CP3).</p>`;
}
