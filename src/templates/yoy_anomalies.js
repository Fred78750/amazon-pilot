// Amazon Pilot — YoY Template : Anomalies catalogue (section 6)
// Implémentation complète : CP3

/**
 * tplAnomalies — Template interprétatif section "Anomalies catalogue"
 * @param {object} dims  — Résultats dimension 12
 * @param {string} sign  — 'negative' | 'positive' | 'stable'
 * @returns {string}     — Paragraphe HTML
 */
function tplAnomalies(dims, sign) {
  // CP3 — placeholder
  return `<p style="color:var(--tx2);font-style:italic">Analyse des anomalies catalogue en cours de rédaction (v3.6.5-CP3).</p>`;
}
